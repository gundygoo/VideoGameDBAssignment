<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.oudersonstudios.videogamedbassignment">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".Activities.QueryAndResultActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.AddModifySeriesActivity" />
        <activity android:name=".Activities.AddModifyVideoGameActivity" />
        <activity android:name=".Activities.AddModifyPublisherActivity" />
        <activity android:name=".Activities.AddModifyPlatformActivity"></activity>
    </application>

</manifest>